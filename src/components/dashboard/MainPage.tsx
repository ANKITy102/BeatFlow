import { FC } from "react";

interface MainPageProps {}

const MainPage: FC<MainPageProps> = ({}) => {
  return (
    <div className="fixed top-0 right-0 h-full w-full bg-red-500 md:pb-24 pb-36 rounded-md m-1 ">
      Hihi
    </div>
  );
};

export default MainPage;
