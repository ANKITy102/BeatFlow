import { FC } from 'react'

interface PlayListInterfaceProps {
  
}

const PlayListInterface: FC<PlayListInterfaceProps> = ({}) => {
  return <div>PlayListInterface</div>
}

export default PlayListInterface